#### 出力結果
0 1 0  
1 1 0

### 理由
```
const c
=
a
++
b
```
は、
```
const c = a;
++b;
```
と等しいため、
cに0が代入された後、bが1となっている。
```
console.log(a, b, c); // => 0 1 0
```
そして以下
```
const e = a++
b;
```
は、
```
const e = a++;
b;
```
と等しいため、
eにaの値0が代入された後にaが1がとなり、bに変動はない。
```
console.log(a, b, e); // => 1 1 0
```